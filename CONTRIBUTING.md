# Contributing

Keep the domain core harness-independent. New Pi-facing behavior belongs under `extensions/` or `skills/`. Any change to knowledge visibility or canon semantics requires regression tests and an ADR when it changes an architectural invariant.
