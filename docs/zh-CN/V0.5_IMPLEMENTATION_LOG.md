# v0.5 Implementation Log

## 关键决策

1. Engine Integration 独立放在 `src/engine-integration.js`，避免 Unity Concern 污染 Narrative Authority。
2. 使用第二个 Pi Extension，不继续膨胀 `narrative-runtime.ts`。
3. 采用 at-least-once + Stable ID + ACK，而不宣称 Exactly-once。
4. ACK Ledger 位于 NarrativeEvent Stream 之外，因为“确认收到”不等于“剧情变化”。
5. Export / Save / Checkpoint 都是派生资产。
6. Project Schema 显式版本化，保留 Legacy Label。
7. v0.5 提供手写 Unity DTO Sketch，Codegen 延后。

验证包括 ACK Idempotency/Unknown ID Rejection、Pending Redelivery、Save Snapshot、Checkpoint Tamper Detection、Migration、Malformed Consequence、Localization、Unity-friendly DTO、CI Validator，以及 GitHub Actions 全仓 40/40 Regression。
